---
trigger: model_decision
description: Apply when generating or modifying a course review application.
---

Read `AGENTS.md`, then `SKILL.md` and `references/output-contract.md`. Use the referenced schema and fixed template. Before delivering an HTML artifact, run `python scripts/validate_output.py <artifact>`; a prose self-check is not a release gate.
